---
title: icalendardb

exclude: ['/scripts', '/Gemfile', '/Makefile', '/README.md', '/CONTRIBUTING.md']

permalink: pretty

collections:
  properties:
    output: true

baseurl: /icalendardb

defaults:
  - scope:
      path: '_properties'
    values:
      layout: property
